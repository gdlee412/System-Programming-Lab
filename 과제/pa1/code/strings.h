// change string to lower case
void toLower(char* destination, char* string);

// return string length excluding NULL
int stringLength(char* string);

// check whether two strings are the same or not
int stringCompare(char* string1, char* string2);

//identifies type of case
int caseIdentifier(char* string);
